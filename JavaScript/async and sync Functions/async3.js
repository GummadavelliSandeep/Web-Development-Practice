function onDone() {
  console.log("Hello EveryOne");
}
setTimeout(onDone, 1000); /*Async Function in javascript and 
There are some files that needs to be wait for example
1 Reading a file
2 sending a network request..ect    */
console.log("Hello,Welcome you all");
// when the thread becomes idle after reaching end then the controll goes to execute the seTimeout async function back
